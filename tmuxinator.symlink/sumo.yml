# ~/.tmuxinator/sumome.yml

name: sumo
root: ~/Code/appsumo/sumome

pre_window: export NODE_ENV=development DEBUG=* && nvm use 0.10.38
startup_window: server

windows:
  - code:
  - build:
  - mysql: mysql sumome
  - redis: redis-cli
  - server: sudo NODE_ENV=development DEBUG=* grunt server
